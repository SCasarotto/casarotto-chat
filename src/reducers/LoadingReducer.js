// import { LoadingKeys } from './../actions/types'

const INITIAL_STATE = {}

export default (state = INITIAL_STATE, action) => {
	// const {} = LoadingKeys

	switch (action.type) {
		default:
			return state
	}
}
